# Contributing

## Prerequisites

- Python 3.11+

## Local setup

```bash
python -m pip install -e .
python -m unittest -v
```

## Pull request checklist

- Keep changes focused and small.
- Add or update tests for behavior changes.
- Ensure `python -m unittest -v` passes locally.
- Update README when CLI flags or behavior changes.

